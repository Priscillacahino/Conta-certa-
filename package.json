{
  "name": "conta-certa",
  "version": "0.11.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "audit:privacy": "node scripts/audit-public-package.mjs",
    "verify": "node --test && node scripts/audit-public-package.mjs"
  }
}
